{
  "name": "@awcp-experiments/04-archive-transport",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Archive transport experiment - tests HTTP-based file transfer",
  "scripts": {
    "build": "echo 'No build needed - using tsx'",
    "start:delegator": "npx tsx start-delegator.ts",
    "start:executor": "npx tsx start-executor.ts",
    "trigger": "npx tsx trigger.ts",
    "clean": "./cleanup.sh"
  },
  "dependencies": {
    "@awcp/sdk": "^0.1.0",
    "@awcp/transport-archive": "^0.1.0"
  },
  "devDependencies": {
    "tsx": "^4.7.0"
  }
}
